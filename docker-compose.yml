version: '3.8'

services:
  ai-service-hub:
    build: .
    container_name: ai-service-hub-container
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
